Rscript -e "renv::restore()"
Rscript -e "lints <- lintr::lint_dir('R'); if(length(lints) > 0) { print(lints); quit(status = 1) } else { message('No lints found in R/.') }"
Rscript -e "lints <- lintr::lint_dir('tests'); if(length(lints) > 0) { print(lints); quit(status = 1) } else { message('No lints found in tests/.') }"
Rscript -e "lints <- lintr::lint_dir('mock_data'); if(length(lints) > 0) { print(lints); quit(status = 1) } else { message('No lints found in mock_data/') }"
Rscript -e "testthat::test_dir('tests')"
